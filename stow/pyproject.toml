[project]
name = "dotfiles-stow"
version = "0.1.0"
description = "Dotfiles management with GNU Stow"
requires-python = ">=3.10"
dependencies = [
    "inquirer>=3.0.0",
]

[project.scripts]
install-stow = "install_stow:main"