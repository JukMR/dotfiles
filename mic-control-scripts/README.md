# Mic Control + Nerd Font Setup

System-wide mic toggle for PipeWire + AwesomeWM font fix.

## Requirements
- PipeWire
- wpctl
- sudo
- curl, unzip

## Install Nerd Font (required for icons)

```bash
./install-font.sh
```
